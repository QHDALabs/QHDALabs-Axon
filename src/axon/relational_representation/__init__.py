"""
Stage 2 — relational representation.

Build the map of relations between perceived items using qhda-core's relational
layer. This is a relation store, NOT a fact store: it records candidate
dependencies, not asserted truths. Must stay fully functional with Qiskit not
installed, mirroring qhda-core's dependency boundary.
"""
